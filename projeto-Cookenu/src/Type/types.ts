

export type authenticationData = {
    id: string
    role: string // o que 'e o role , e para que serve???
}

export type userController = {
    name: string,
    email: string,
    password: string
}